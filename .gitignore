# Godot 4+ specific ignores
.godot/
/android/

# Godot-generated import/cache files
*.translation
export.cfg

# Local export build output (the CI workflow writes APKs into build/, we
# don't want to commit built binaries)
/build/
